<% @users.each do |user| %>
  <%= user.name %>
  <%= link_to "プロフィール編集", admin_user_path(user)%><br>
<% end %>

<%=link_to "新規ユーザー登録", new_admin_user_path%>

<%= month_calendar do |date| %>
  <%= date.day %>
  <%= shift_number_on_day(date)%> 
<% end %>